# YouTube Comment NLP Analysis

Pipeline:
- Fetch YouTube comments (YouTube Data API v3)
- Preprocess text (cleaning, stopwords)
- Analyze (word frequencies, wordcloud, sentiment/topic options)

## Setup
python -m venv yt_env
source yt_env/bin/activate
pip install -r requirements.txt
